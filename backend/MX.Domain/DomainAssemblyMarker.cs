namespace MX.Domain;

/// <summary>
/// Reflection anchor for this assembly. Carries no behaviour — it exists so tests
/// and DI scanning can reference <c>MX.Domain</c> without depending on a real type
/// whose location might later change.
/// </summary>
public sealed class DomainAssemblyMarker;
